{
  "name": "spacetaxi-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@noble/ciphers": "^1.0.0",
    "hash-wasm": "^4.11.0"
  }
}
